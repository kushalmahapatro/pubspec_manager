name: example_prod
description: A new Flutter Dev project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 0.0.1+1

dependencies:
  desktop_multi_window: ^0.2.0,
  firebase_crashlytics: ^3.3.3

